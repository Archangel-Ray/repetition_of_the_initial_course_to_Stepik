# The input of the program is a real number. It is necessary to import the math module,
# call the ceil function of this module for the entered number, and display the result on the screen.

import math


x = float(input("real number: "))
print(math.ceil(x))
