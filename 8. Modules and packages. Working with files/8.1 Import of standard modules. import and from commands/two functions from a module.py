# Import only two functions from the random module: seed and randint. Then, in the program, execute them, as follows:
#
# seed(1)
# print(randint(10, 50))


from random import seed, randint

seed(1)
print(randint(10, 50))
