# The input of the program is a real number. It is necessary to import only one floor function from the math module,
# call it on the entered number, and display the result on the screen.

from math import floor


x = float(input("real number: "))
print(floor(x))
