in this lesson we do not solve examples, but there is a quiz:

1) What does the pip list command do?
    >> displays a list of installed modules for the current interpreter

2) What commands can be used to install third party packages?
    >> pip install Django==2.1.2
    >> pip install -r requirements.txt
    >> pip install Django

3) What the team does:
    pip freeze requirements.txt
    >> creates a text file with a list of installed modules and their version numbers

4) What is the purpose of visiting the https://pypi.org site?
    >> to search for third-party modules (for their subsequent installation)
