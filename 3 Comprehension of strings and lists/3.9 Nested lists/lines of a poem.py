# Three lines of the poem are introduced (each on a new line).
# Save it as a nested list broken down by lines and words
# (words are separated by a space).
# Display the resulting list lst with the command:
#
# print(lst)

lst = [input('type the first line of the poem: ').split(), input('write the next line of the poem: ').split(), input('write the last line of the poem: ').split()]
print(lst)
