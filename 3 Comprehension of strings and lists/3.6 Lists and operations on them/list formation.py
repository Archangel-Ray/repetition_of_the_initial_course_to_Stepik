# Enter three integers on one line separated by a space.
# Form a list lst that stores these values ​​in the order
# they were entered.
# Display the result on the screen using the command:
#
# print(lst)

lst = list(map(int, input('enter numbers: ').split()))
print(lst)
