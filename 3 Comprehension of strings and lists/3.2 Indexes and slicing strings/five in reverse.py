# From the entered string, display the first five characters in reverse order. The input string is assumed to have at least five characters.

string = input('enter a string of at least five characters: ')
print(string[:5][::-1])
