# Write a program to display the last three characters from the input string. We will assume that the string is guaranteed to be at least three characters long.

string = input('enter the string: ')
print(string[-3:])
