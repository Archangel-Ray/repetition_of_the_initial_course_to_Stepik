# Write a program to display the first four characters from the input string. We will assume that the string is guaranteed to be at least four characters long.

string = input('enter the string: ')
print(string[:4])
